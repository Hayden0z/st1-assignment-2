


1.Easy to understand

Human version:has comments that tell you what the code does. The code is straight forward there is no inputs its sets all the variables and just prints them. 

Ai version:has no comments that tell a user what's happening and requires the user to decipher the code. It is more complex in that it has error checking, inputs and function so is more complex. 


2.Runs successfully

Human version:code works and shows all outputs.

Ai version:code doesn't work properly, it does not show any errors nor outputs. It does not ask for input. This would be becasue the prompt asked for code to create a simple beginner-friendly Python function that stores patient name, practitioner name and
appointment time. In that instrcuction it did not decide that input or were necessarry only store. This is a key limitation of AI as it needs to be told explicity what is needed it created a program to run data without inputs or outputs.


3.Uses only required features

Human version:The human version met the requirements for input, ouput, using lists, ditionaries and functions. I did want to add a while loop but did not.

Ai version:uses the main features however it was told to be very simple program and was probithed to make the prgram more advanced and coudn't make it into a database or GUI. It missed the requiremetns for input and output, dictionary.


4.Adds assumptions

Human version:I assumed that storing appointments in a Python list was acceptable and did not require a database and that the data disappearing when the program ends was fine for this task. I  assumed that the appointment time could be stored as a simple string instead of a real date/time and that only the patient name needed validation and that the practitioner and time did not require it.

Ai version: Assumed that the code needed to be very simple including basic validation.It  assumed that the appointment time can be e string and that appointments can be stored in a  list. It assumes that it is acceptable for all data to  be lost when the program ends.


5.Handles errors

Human version:checks that the patients name isn't empty, if it is empty, the function raises a ValueError.

Ai version: checks all three being patient name, practitioner name, and appointment time. To ensure they are not empty and will raise an error if they are.


6.Could I explain it?

Human version:Yes at the start, the first patient, practitioner, and appointment time are inputed and stored and then displayed using a formatted string so it displays reasonably. The same process is repeated a second time with the prestored value to show two different appointments

Ai version:The AI version checks that the variables are not empty then stores each appointment as a dictionary in a list, it does not work properly as it does not print the output.